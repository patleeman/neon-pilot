{
  "schemaVersion": 2,
  "id": "system-template-crud-page",
  "name": "Template: CRUD Page",
  "description": "Starter template — main-only management page with a list view and inline editor flow. Full create/edit/delete.",
  "version": "0.1.0",
  "defaultEnabled": false,
  "frontend": {
    "entry": "dist/frontend.js"
  },
  "backend": {
    "entry": "dist/backend.mjs",
    "actions": [
      { "id": "templateItemsList", "handler": "list", "title": "List items", "worker": { "enabled": true } },
      { "id": "templateItemsGet", "handler": "get", "title": "Get item", "worker": { "enabled": true } },
      { "id": "templateItemsSave", "handler": "save", "title": "Save item", "worker": { "enabled": true } },
      { "id": "templateItemsDelete", "handler": "delete", "title": "Delete item", "worker": { "enabled": true } }
    ]
  },
  "contributes": {
    "views": [
      {
        "id": "page",
        "title": "Template: CRUD Page",
        "location": "main",
        "route": "/ext/template-crud-page",
        "component": "CrudPage",
        "activation": "on-route"
      }
    ],
    "nav": [
      {
        "id": "nav",
        "label": "CRUD Page",
        "route": "/ext/template-crud-page",
        "pageType": "table",
        "icon": "kanban"
      }
    ]
  },
  "permissions": []
}
