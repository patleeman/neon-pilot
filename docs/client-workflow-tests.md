# Client Workflow Tests

This is the source map for frontend tests that protect user-visible Neon Pilot workflows. These tests should exercise product outcomes across client state, API snapshots, routing, and extension registries instead of only testing isolated helpers.

Use focused client workflow tests before relying on Electron smoke tests. Electron smoke should prove packaging and native wiring; client workflow tests should catch most regressions quickly in `vitest`.

## Coverage Principles

- Test the same state transitions users depend on: create, navigate, refresh, reopen, resize, enable, disable, save, and recover.
- Mock at the API/SSE boundary, not inside the component under test, when the workflow depends on backend timing.
- Assert visible or route-level outcomes: sidebar rows, active route, selected panel, enabled controls, persisted layout, and rendered extension surfaces.
- Include stale snapshot, loading, empty, and error cases when a workflow can cross async boundaries.
- Keep workflow tests deterministic and narrow enough to run in focused package checks.

## Workflow Matrix

| Area         | Workflow                                                                                                                                                                        | Expected coverage                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Chat primary | Draft prompt creates a reserved conversation, opens `/conversations/:id`, keeps the sidebar row through snapshot refresh and stale remote layout sync                           | `ui/src/pages/ConversationPage.lazyComposerMetadata.test.tsx` covers the draft submit route handoff, reserved session-file live creation, and delayed tab/sidebar refresh. `ui/src/hooks/useConversations.test.tsx` covers the sidebar model.                                                                                                                                                                                                                             |
| Chat primary | Existing conversation opens from sidebar, updates active id, and survives reload hydration                                                                                      | `ui/src/app/App.sidebarNavigation.test.tsx` covers the full App/Layout/Sidebar route workflow, including clicking an existing row, updating `/conversations/:id`, marking the active row, and remounting on that route. `ui/src/components/Sidebar.nested.test.tsx` covers rendered remote layout hydration and local persistence.                                                                                                                                        |
| Chat primary | Close, reopen, pin, unpin, archive, and restore preserve the expected shelves and active route                                                                                  | `ui/src/app/App.sidebarNavigation.test.tsx` covers archiving the active route and reopening the most recent archived conversation back to `/conversations/:id`; `ui/src/components/Sidebar.nested.test.tsx` covers rendered sidebar close/archive/pin/lock/reopen shortcut workflows; `sessionTabs`/`useConversations` tests cover primitives.                                                                                                                            |
| Chat primary | Live title, running state, pending prompt, and queued prompt state render consistently in sidebar/header/composer                                                               | `ui/src/app/App.chatPrimaryWorkflow.test.tsx` covers an active conversation route where the page pushes a live title, pending and queued prompt UI stays visible, a queued execution marks the sidebar as background work, and a running metadata event upgrades that same row to the running state. Existing hook/component tests cover lower-level pieces.                                                                                                              |
| Chat primary | Transcript scrolling respects reader intent while messages stream                                                                                                               | `ui/src/hooks/useConversationScroll.test.ts` covers turn-start anchoring, explicit Jump to latest follow mode, keyboard/scroll detachment, streaming tail growth, and historical prepend restoration. `ui/src/conversation/conversationScroll.test.ts` covers pure scroll anchors and visibility helpers.                                                                                                                                                                 |
| Extensions   | Registry loads nav items, opens extension routes, invokes backend actions with real frontend context shape, and handles action failures                                         | `ui/src/app/App.sidebarNavigation.test.tsx` covers app-shell primary extension nav from the real Sidebar into the extension route host plus reload hydration. `ui/src/extensions/ExtensionPage.test.tsx` covers route host selection; `extensions/system-extension-manager/src/frontend.test.tsx` covers installed-row enable/update/reinstall/delete workflows and failures.                                                                                             |
| Extensions   | Sidebar extension views temporarily replace native Threads body without breaking native sidebar restore                                                                         | `ui/src/components/Sidebar.test.tsx` covers extension sidebar replacement and native thread restore after returning to chat.                                                                                                                                                                                                                                                                                                                                              |
| Settings     | Settings nav opens provider and desktop panels, edits constrained controls, persists through reload, and shows validation errors                                                | `ui/src/app/App.sidebarNavigation.test.tsx` covers app-shell settings navigation from the real Sidebar settings nav into the extension route host plus reload hydration on `/settings`. `ui/src/pages/SettingsPage.providers.test.tsx` covers rendered settings sidebar hash navigation into provider sections and provider editor workflows; `ui/src/pages/SettingsPage.desktopConnections.test.tsx` covers desktop panel persistence and duplicate shortcut validation. |
| Geometry     | Sidebar/workbench resizing, layout mode changes, tab rail visibility, and route transitions preserve bounded dimensions                                                         | `ui/src/components/Layout.workbenchToggle.test.tsx` covers visible workbench mode transitions, tab rail placement, restored sidebar/workbench document widths on rendered conversation routes, and user drag-resize persistence across conversation route transitions; `ui-state/layoutSizing.test.ts` covers width bounds.                                                                                                                                               |
| Launcher     | Opens as a top-bar dropdown, shows and edits pinned applications/pages/conversations, uses contributed icons, searches commands and resources, and supports keyboard navigation | `ui/src/components/DesktopTopBar.interaction.test.tsx` covers the app-chrome control opening the anchored launcher; `ui/src/components/CommandPalette.test.tsx` covers its dropdown geometry, pins, contributed page identity, command availability, handled/unhandled actions, and conversation navigation; `commands/commandPalette*.test.ts` covers search, selection models, and route builders.                                                                      |
| Recovery     | Stale snapshots, missing session metadata, failed backend calls, and reconnects preserve valid local UI while surfacing errors                                                  | `ui/src/app/App.recoveryWorkflow.test.tsx` covers the route-level reconnecting state and failed sessions refresh while preserving the active conversation route and sidebar row; `ui/src/hooks/useConversations.test.tsx` covers stale remote layout, out-of-order metadata, full-snapshot races, and failed row metadata preserving placeholders.                                                                                                                        |

## Required Checks

For client workflow changes, run the focused `vitest` file first, then the affected desktop package build or UI build when app shell code changes. If a behavior is reachable through the desktop app, follow with an Electron/app-path smoke or explicitly record the blocker.
