import { describe, expect, it } from 'vitest';

describe('localApi startup', () => {
  it('keeps knowledge-base sync out of the desktop local API runtime', () => {
    expect(true).toBe(true);
  });
});
