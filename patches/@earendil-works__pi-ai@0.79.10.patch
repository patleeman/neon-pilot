diff --git a/dist/providers/openai-responses-shared.js b/dist/providers/openai-responses-shared.js
index 3d970f38df3030a2528afac242647334d81e7e4f..301b035d0397985b11a909eb1428397ef9279881 100644
--- a/dist/providers/openai-responses-shared.js
+++ b/dist/providers/openai-responses-shared.js
@@ -314,7 +314,7 @@ export async function processResponsesStream(openaiStream, output, stream, model
         else if (event.type === "response.output_text.delta") {
             if (currentItem?.type === "message" && currentBlock?.type === "text") {
                 if (!currentItem.content || currentItem.content.length === 0) {
-                    continue;
+                    currentItem.content = [{ type: "output_text", text: "", annotations: [] }];
                 }
                 const lastPart = currentItem.content[currentItem.content.length - 1];
                 if (lastPart?.type === "output_text") {
@@ -332,7 +332,7 @@ export async function processResponsesStream(openaiStream, output, stream, model
         else if (event.type === "response.refusal.delta") {
             if (currentItem?.type === "message" && currentBlock?.type === "text") {
                 if (!currentItem.content || currentItem.content.length === 0) {
-                    continue;
+                    currentItem.content = [{ type: "refusal", refusal: "" }];
                 }
                 const lastPart = currentItem.content[currentItem.content.length - 1];
                 if (lastPart?.type === "refusal") {
@@ -393,8 +393,8 @@ export async function processResponsesStream(openaiStream, output, stream, model
                 currentBlock = null;
             }
             else if (item.type === "message" && currentBlock?.type === "text") {
-                currentBlock.text =
-                    item.content?.map((c) => (c.type === "output_text" ? c.text : c.refusal)).join("") || "";
+                const finalText = item.content?.map((c) => (c.type === "output_text" ? c.text : c.refusal)).join("") || "";
+                currentBlock.text = finalText || currentBlock.text;
                 currentBlock.textSignature = encodeTextSignatureV1(item.id, item.phase ?? undefined);
                 stream.push({
                     type: "text_end",
