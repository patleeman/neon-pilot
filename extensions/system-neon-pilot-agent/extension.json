{
  "schemaVersion": 2,
  "id": "system-neon-pilot-agent",
  "name": "Neon Pilot Agent",
  "packageType": "system",
  "defaultEnabled": true,
  "description": "Expose Neon Pilot as a CLI delegated agent runtime. MCP admin exposure is deprecated; use the neon-pilot CLI externally and the canonical neon_pilot tool internally.",
  "version": "0.1.0",
  "frontend": {
    "entry": "dist/frontend.js",
    "styles": []
  },
  "backend": {
    "entry": "src/backend.ts",
    "actions": [
      {
        "id": "neonPilotAgent",
        "handler": "neonPilotAgent",
        "title": "Neon Pilot Agent",
        "worker": {
          "enabled": true
        }
      },
      {
        "id": "readSettings",
        "handler": "readSettings",
        "title": "Read Neon Pilot Agent settings",
        "worker": {
          "enabled": true
        }
      },
      {
        "id": "updateSettings",
        "handler": "updateSettings",
        "title": "Update Neon Pilot Agent settings",
        "worker": {
          "enabled": true
        }
      }
    ],
    "protocolEntrypoints": [
      {
        "id": "neon-pilot-agent",
        "handler": "neonPilotAgentCli",
        "title": "Neon Pilot Agent CLI",
        "description": "Command-line interface for delegating tasks to Neon Pilot and inspecting durable runs."
      },
      {
        "id": "neon-pilot-agent-mcp",
        "handler": "neonPilotAgentMcp",
        "title": "Neon Pilot Agent MCP Server (deprecated)",
        "description": "Deprecated admin surface. Use the neon-pilot CLI for external agents and the canonical neon_pilot tool for internal agents."
      }
    ]
  },
  "contributes": {
    "cliCommands": [
      {
        "id": "bootstrap-doctor",
        "command": "bootstrap doctor",
        "description": "Check whether Neon Pilot is ready for external agent control.",
        "action": "neonPilotAgent",
        "jsonDefault": true
      },
      {
        "id": "bootstrap-configure",
        "command": "bootstrap configure",
        "description": "Configure Neon Pilot defaults and external-agent entrypoints.",
        "action": "neonPilotAgent",
        "jsonDefault": true
      },
      {
        "id": "bootstrap-defaults-set",
        "command": "bootstrap defaults set",
        "description": "Set default provider, model, cwd, thinking level, and service tier.",
        "action": "neonPilotAgent",
        "jsonDefault": true
      },
      {
        "id": "bootstrap-provider-set-key",
        "command": "bootstrap provider set-key",
        "description": "Store a provider API key through the configured secrets backend. Use --stdin.",
        "action": "neonPilotAgent",
        "jsonDefault": true
      },
      {
        "id": "bootstrap-provider-save",
        "command": "bootstrap provider save",
        "description": "Create or update a model provider definition.",
        "action": "neonPilotAgent",
        "jsonDefault": true
      },
      {
        "id": "bootstrap-provider-model",
        "command": "bootstrap provider model",
        "description": "Create or update a provider model definition.",
        "action": "neonPilotAgent",
        "jsonDefault": true
      }
    ],
    "settingsComponent": {
      "id": "neon-pilot-agent",
      "component": "NeonPilotAgentSettingsPanel",
      "sectionId": "neon-pilot-agent-capabilities",
      "label": "Neon Pilot Agent",
      "description": "Enable or disable external Neon Pilot agent entrypoints. MCP admin exposure is deprecated; prefer the neon-pilot CLI.",
      "order": 45
    },
    "skills": [
      {
        "id": "neon-pilot-control",
        "path": "skills/neon-pilot-control/SKILL.md",
        "title": "Neon Pilot Control"
      }
    ]
  },
  "permissions": [
    "agent:run",
    "agent:conversations",
    "runs:read",
    "runs:write",
    "settings:read",
    "settings:write"
  ]
}
