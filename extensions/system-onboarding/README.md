# Onboarding Extension

Creates the first-run welcome conversation for new Neon Pilot installs.

The onboarding conversation explains the first useful steps: configure a provider in Settings, understand that Neon Pilot is extension-based, manage extensions from Settings → Extensions, inspect agent-facing context from Agent Capabilities, then start a real task conversation.
