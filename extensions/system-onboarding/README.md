# Onboarding Extension

Creates the first-run welcome conversation for new Neon Pilot installs.

The onboarding conversation explains the first useful steps: configure a provider in Settings, understand that Neon Pilot is extension-based, manage runtime capabilities from Settings → Agent Runtime, then start a real task conversation.
